name: Apply accepted RC84 source snapshot
on:
  push:
    branches: [sync-rc84-accepted]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sync-rc84-accepted
      - name: Apply exact source snapshot
        shell: bash
        run: |
          if [ ! -f .sync/rc84_source.zip.b64 ]; then exit 0; fi
          base64 -d .sync/rc84_source.zip.b64 > /tmp/rc84-source.zip
          unzip -o /tmp/rc84-source.zip -d .
          rm -rf .sync
          rm -f .github/workflows/apply-rc84-sync.yml
          git config user.name "content-creator-sync"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "RC84: sync accepted source snapshot"
          git push origin HEAD:sync-rc84-accepted
